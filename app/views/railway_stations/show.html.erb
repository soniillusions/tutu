<p style="color: green"><%= notice %></p>

<%= render @railway_station %>

<% if @railway_station.routes.nil? %>
  <p>Станция не имеет ни одного маршрута!</p>
<% else %>
  <p>Станция входит в следующие маршруты:</p>
  <% @railway_station.routes.each do |route| %>
    <li><%= route.name %></li>
  <% end %>
<% end %>

<div>
  <%= link_to "Edit this railway station", edit_railway_station_path(@railway_station) %> |
  <%= link_to "Back to railway stations", railway_stations_path %>

  <%= button_to "Destroy this railway station", @railway_station, method: :delete %>
</div>

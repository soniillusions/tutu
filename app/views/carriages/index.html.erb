<%= link_to 'Назад', train_path(@train) %>

<%= render partial: 'trains/train', locals: { train: @train } %>

<div class="mb-2">
  <%= link_to 'Новый вагон', new_train_carriage_path(@train), class: 'btn btn-secondary' %>
</div>

<% @carriages.each do |car| %>
  <%= render partial: 'carriage', locals: { carriage: car } %>
<% end %>